[project]
name = "mtcli-vprofile"
version = "0.1.0"
description = "Plugin mtcli que exibe o volume profile"
authors = [
    {name = "Valmir França da Silva",email = "vfranca3@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.10,<3.14.0"
dependencies = [
    "mtcli (>=1.19.4,<2.0.0)"
]

[project.entry-points."mtcli.plugins"]
volume = "mtcli_vprofile.vprofile:volume"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
